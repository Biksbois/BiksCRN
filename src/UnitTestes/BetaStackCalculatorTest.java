package UnitTestes;

import org.junit.jupiter.api.Test;
import simpleAdder.interpret.TypeCheckers.BetaStackCalculator;

import java.text.DecimalFormat;
import java.util.Stack;

import static org.junit.jupiter.api.Assertions.*;

class BetaStackCalculatorTest {
/*
    BetaStackCalculator BS = new BetaStackCalculator();
    TestGetMethods get = new TestGetMethods();

    @Test
    void calculate() {
        DecimalFormat df = new DecimalFormat("#.####");

        Stack<String> expr = get.InitializedStack();

        float ExpectedResult = 7.55555f;
        float ActualResult = BS.Calculate(expr);

        assertEquals(df.format(ExpectedResult), df.format(ActualResult));
    }

 */
}